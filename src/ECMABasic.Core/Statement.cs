namespace ECMABasic.Core
{
    /// <summary>
    /// The full list of statements built in to BASIC-55.
    /// </summary>
    public enum StatementType
    {
        END,
        DATA,
        DEF,
        DIMENSION,
        GOSUB,
        GOTO,
        IF_THEN,
        INPUT,
        LET,
        ON_GOTO,
        OPTION,
        PRINT,
        RANDOMIZE,
        READ,
        REMARK,
        RESTORE,
        RETURN,
        STOP
    }
}
